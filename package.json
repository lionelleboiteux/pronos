{
  "name": "jeu-des-pronos",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Jeu des Pronos - red-gate test suite (tests only; no production code yet).",
  "scripts": {
    "setup:contract": "python3 -m venv .venv-contract && .venv-contract/bin/pip install --disable-pip-version-check schemathesis==4.25.2",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit tests/telemetry",
    "test:db": "vitest run tests/db",
    "test:contract": "vitest run tests/contract",
    "test:e2e": "vitest run tests/e2e"
  },
  "devDependencies": {
    "@stoplight/prism-cli": "^5.14.2",
    "@testcontainers/postgresql": "^12.0.4",
    "@types/node": "^22.10.2",
    "@types/pg": "^8.20.3",
    "@vitest/coverage-v8": "^2.1.9",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "fast-check": "^3.23.2",
    "pg": "^8.22.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "yaml": "^2.6.1"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
